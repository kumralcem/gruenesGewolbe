# Format Stability Starts with Dogfooding

The prototype has no real vaults, so the first usable desktop milestone may replace format version 1 and its item-record shape rather than carrying compatibility code for disposable test data. Compatibility obligations begin when the first real dogfood vault is created; subsequent incompatible migrations require explicit backup and confirmation.

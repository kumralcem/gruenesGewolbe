# Tauri Desktop Shell

The first user interface should be a Tauri desktop app rather than a browser extension, Electron app, or pure native Rust UI. Tauri fits the goal of exploring local desktop development with Rust while still allowing a productive web-based interface, and it preserves the architectural requirement that the vault remains ordinary local files rather than app-owned storage.

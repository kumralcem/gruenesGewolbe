# Metadata Search Before Vector Search

The first milestone should implement metadata text search before vector search. Search should initially cover item titles, creators, years, tags, saving reasons, summaries, source URLs, original filenames, and collection names; vector search remains an important later capability, but it should not block the first save, import, and browsing loop.

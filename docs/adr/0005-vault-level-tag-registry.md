# Vault-Level Tag Registry

The vault should include a tag registry that records known tags, aliases, and meanings. Item creation should consult this registry before adding tags, so the system can stay open-ended without relying on later maintenance passes to merge obvious duplicates.

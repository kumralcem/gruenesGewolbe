# Minimal CLI Over Archive Core

The first milestone should include a minimal CLI over the archive core for debugging, agent access, and basic automation, but it should not become a full parallel interface to the desktop app. Initial commands can cover opening/validating a vault, importing a paintings folder, rebuilding indexes, searching metadata, and inspecting item records; richer agent workflows, batch enrichment, and maintenance commands can be added later.

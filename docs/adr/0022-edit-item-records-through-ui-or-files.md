# Edit Item Records Through UI or Files

Users should be able to edit item records through the app UI for common fields such as title, creator, year, summary, saving reason, tags, collections, review status, and source links, while direct file edits remain valid because the item record is canonical. The app should detect or rescan changed records and refresh derived indexes rather than treating the UI as the only safe editing path.

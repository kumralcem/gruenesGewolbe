# Markdown Item Records

Saved items use a Markdown item record with structured frontmatter rather than storing canonical metadata only in a database or opaque file. This keeps the vault readable and useful to people, scripts, and agents while still allowing the app to build faster derived indexes for search and browsing.

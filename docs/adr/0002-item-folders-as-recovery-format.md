# Item Folders as Recovery Format

Each saved item should live in an ordinary local folder with its files and readable record, but the folder layout is optimized for durability and recovery rather than day-to-day manual browsing. The app interface and rebuildable indexes provide the polished browsing and search experience, while item folders preserve ownership if the app disappears.

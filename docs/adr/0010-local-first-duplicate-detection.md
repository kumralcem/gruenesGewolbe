# Local-First Duplicate Detection

Duplicate detection should be local-first and cheap during capture. The app may store exact hashes, perceptual hashes, dimensions, source links, and extracted metadata, but paid AI calls should be reserved for enrichment or ambiguous duplicate candidates rather than required for every saved item.

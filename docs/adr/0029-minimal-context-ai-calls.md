# Minimal Context AI Calls

AI calls should receive only the minimum files and text needed for the selected action. Idea summarization should send cleaned text rather than raw HTML, image tagging should send the relevant image or preview, and enrichment should avoid uploading whole vaults, unrelated item records, or full source copies unless a user explicitly requests a broader operation.

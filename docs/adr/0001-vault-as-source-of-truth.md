# Vault as Source of Truth

The vault is the source of truth for the personal archive. The app may build databases, indexes, caches, or embeddings to make the archive searchable and useful, but those derived stores must be rebuildable from the local vault files because portability and long-term ownership matter more than treating the app database as canonical.

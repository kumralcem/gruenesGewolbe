# Offline Core With AI Enhancement

The app should be useful without an AI provider or API key. Users must be able to browse existing vaults, save new items, preserve files and source links, enter manual summaries and tags, run basic search, and use local duplicate signals offline; AI unlocks richer summaries, tag suggestions, embeddings, and enrichment rather than defining the core archive.
